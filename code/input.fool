/*

let
    A x = null;
in
    x = new A();


let
    int f(int par) if (par == 0) then {print(1)} else { f(par-1) };
in
    f(5);

let
    int f()
        let
            int x = 2;
        in
            print(x);
in
    f();

*/



let
    void f(int x) print(x);
    int f (int y, int z) return 0;
in
    f(2);
